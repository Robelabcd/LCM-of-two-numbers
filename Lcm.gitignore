#include <stdio.h>
int lcm() {
  int x, y, maximum;
  printf("Enter two positive integers: ");
  scanf("%d %d", &x, &y);
  maximum = (x > y) ? x : y;
    
  while (1) {
      if (res % x == 0 && maximum % y == 0) {
          printf("The LCM of %d and %d is %d.", x, y, maximum);
          break;
      }
      ++maximum;
  }
}

int main() 
{
   lcm(); 
   return 0; 
}
